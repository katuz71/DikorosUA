# 🚀 Быстрый старт перед продакшеном

## ✅ Что уже готово

- ✅ .env файл создан
- ✅ Все критические проблемы исправлены
- ✅ Безопасность настроена
- ✅ Логирование работает

---

## 📋 Что нужно сделать СЕЙЧАС

### 1. ⚠️ КРИТИЧНО: Обновить PROD_API_URL

Откройте файл `app/config/api.ts` и на строке 18 замените:

```typescript
const PROD_API_URL = 'https://yourdomain.com'; // ← ЗАМЕНИТЕ ЭТО!
```

На ваш реальный домен, например:
```typescript
const PROD_API_URL = 'https://api.vitastore.com';
```

**Без этого приложение не будет работать в продакшене!**

---

### 2. Проверить .env файл

Убедитесь что в `.env` есть:

```bash
ENVIRONMENT=production
NOVA_POSHTA_API_KEY=ваш_ключ_здесь
ALLOWED_ORIGINS=https://ваш-домен.com
```

---

### 3. Установить новые зависимости

```bash
pip install slowapi==0.1.9
```

Или переустановить все:
```bash
pip install -r requirements.txt
```

---

### 4. Протестировать сервер

```bash
python main.py
```

Должен запуститься без ошибок. Проверьте что:
- ✅ Сервер запустился на порту 8001
- ✅ Нет ошибок в консоли
- ✅ `/health` эндпоинт работает

---

## 🎯 Для продакшена (если деплоите на сервер)

### 5. Настроить домен

- Купить/настроить домен
- Настроить DNS (A запись на IP сервера)
- Установить SSL (Let's Encrypt)

### 6. Собрать приложение

```bash
# Android
npx expo build:android

# iOS  
npx expo build:ios
```

---

## ✅ Минимальный чеклист

- [ ] PROD_API_URL обновлен в `app/config/api.ts`
- [ ] `.env` содержит `ENVIRONMENT=production`
- [ ] `.env` содержит `NOVA_POSHTA_API_KEY`
- [ ] `.env` содержит `ALLOWED_ORIGINS` (ваш домен)
- [ ] `pip install -r requirements.txt` выполнен
- [ ] Сервер запускается без ошибок

---

## 🎉 Готово!

После выполнения этих шагов проект готов к продакшену.

**Самый важный шаг:** Обновите `PROD_API_URL` в `app/config/api.ts`!


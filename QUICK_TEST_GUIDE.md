# 🚀 БЫСТРЫЙ СТАРТ: Тестирование вариантов товаров

## Шаг 1: Запуск Backend

```bash
# Перейди в директорию проекта
cd c:\Work\DikorosUA

# Запусти сервер
python main.py
```

Сервер должен запуститься на `http://localhost:8001`

---

## Шаг 2: Тест Backend API (Node.js)

```bash
# В новом терминале запусти тестовый скрипт
node test_variants_api.js
```

**Ожидаемый результат:**
```
✅ Список товаров: ✅ PASS
✅ Детальная информация: ✅ PASS
✅ Структура данных: ✅ PASS
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ
```

---

## Шаг 3: Запуск Frontend

```bash
# Запусти Expo приложение
npm start
```

Выбери платформу (Android/iOS/Web)

---

## Шаг 4: Тест Frontend UI

### Вариант A: Через тестовый экран (рекомендуется)

1. Открой приложение
2. Перейди во вкладку "Test Variants" (если добавил в навигацию)
3. Нажми "🚀 Запустить все тесты"
4. Проверь результаты

### Вариант B: Ручное тестирование

1. **Каталог товаров:**
   - Открой главную страницу
   - Найди товар с вариантами (например, "Чага березова")
   - Проверь: отображается "від X ₴"

2. **Страница товара:**
   - Нажми на товар с вариантами
   - Проверь: отображаются селекторы вариантов
   - Переключи варианты
   - Проверь: цена меняется

3. **Корзина:**
   - Добавь несколько вариантов одного товара
   - Открой корзину
   - Проверь: разные варианты = разные строки

4. **Оформление заказа:**
   - Оформи тестовый заказ
   - Проверь в админке: варианты отображаются

---

## Шаг 5: Проверка в браузере (опционально)

```javascript
// Открой DevTools (F12)
// Вставь в консоль:

fetch('http://localhost:8001/products')
  .then(r => r.json())
  .then(products => {
    console.log('📦 Total products:', products.length);
    
    const withVariants = products.filter(p => p.variants && p.variants.length > 0);
    console.log('📊 With variants:', withVariants.length);
    
    if (withVariants.length > 0) {
      console.log('📝 Example:', withVariants[0]);
    }
  });
```

---

## 📋 Быстрый чеклист

```
Backend:
[ ] Сервер запущен на :8001
[ ] GET /products работает
[ ] Товары содержат variants
[ ] Variants это массив объектов

Frontend:
[ ] Приложение запущено
[ ] Каталог отображает "від X ₴"
[ ] Страница товара показывает селектор
[ ] Цена меняется при выборе
[ ] Корзина сохраняет варианты

Integration:
[ ] Можно добавить товар с вариантом в корзину
[ ] Можно оформить заказ
[ ] Заказ создается успешно
```

---

## 🐛 Частые проблемы

### Backend не запускается
```bash
# Проверь что установлены зависимости
pip install -r requirements.txt

# Проверь порт
# Если :8001 занят, измени в main.py и config/api.ts
```

### Frontend не подключается к API
```typescript
// Проверь config/api.ts
export const API_URL = 'http://localhost:8001'; // для локальной разработки
```

### Variants = undefined
```python
# В main.py проверь что variants парсятся:
if row["variants"]:
    product["variants"] = json.loads(row["variants"])
```

### Тестовый экран не отображается
```bash
# Убедись что файл создан:
app/(tabs)/test-variants.tsx

# Или используй test_variants_api.js
node test_variants_api.js
```

---

## 📊 Ожидаемые результаты

После успешного прохождения всех тестов:

✅ Backend возвращает ~98 товаров  
✅ ~20-30 товаров имеют варианты  
✅ Парсер определяет режим (complex/simple/none)  
✅ UI корректно отображает варианты  
✅ Корзина работает с вариантами  
✅ Заказ создается успешно  

---

## 📞 Если что-то не работает

1. **Проверь логи сервера** - там будут детали ошибок
2. **Проверь консоль браузера/DevTools** - там React ошибки
3. **Запусти test_variants_api.js** - это покажет проблемы с API
4. **Проверь Network tab** - там видны HTTP запросы
5. **Перечитай VARIANTS_IMPLEMENTATION_REPORT.md** - там все детали

---

## 🎉 После успешных тестов

1. Заполни чеклист в отчете
2. Сделай скриншоты работающего UI
3. Протестируй на реальных устройствах
4. Собери фидбек от команды
5. Деплой в production! 🚀

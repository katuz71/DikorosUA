# 🚀 Руководство по деплою в продакшен

## ✅ Что было исправлено

### 1. Безопасность
- ✅ API ключ Nova Poshta перемещен в переменные окружения
- ✅ SQL Injection уязвимости исправлены
- ✅ CORS настроен для продакшена (ограничен конкретными доменами)
- ✅ Добавлен rate limiting для защиты от DDoS

### 2. Конфигурация
- ✅ Настроена конфигурация для разных окружений (dev/prod)
- ✅ API_URL настраивается через переменные окружения
- ✅ Создан `.env.example` для примера

### 3. Логирование
- ✅ Заменен `print()` на `logging` в Python
- ⚠️ `console.log` в frontend оставлены (можно убрать позже)

### 4. Валидация
- ✅ Добавлена валидация размера файлов (максимум 10 MB)
- ✅ Добавлена валидация расширений файлов

---

## 📋 Шаги для деплоя

### 1. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Environment
ENVIRONMENT=production

# API Configuration
API_URL=https://yourdomain.com

# CORS (для продакшена)
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Nova Poshta API
NOVA_POSHTA_API_KEY=your_nova_poshta_api_key_here

# Telegram Bot (опционально)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Monobank Payment (опционально)
MONOBANK_API_TOKEN=your_monobank_api_token_here

# OpenAI API (для чата)
OPENAI_API_KEY=your_openai_api_key_here
```

### 2. Установка зависимостей

```bash
# Backend
pip install -r requirements.txt

# Frontend
npm install
```

### 3. Настройка Frontend

В файле `app/config/api.ts` замените:

```typescript
const PROD_API_URL = 'https://yourdomain.com'; // ЗАМЕНИТЕ НА ВАШ ДОМЕН
```

Или используйте переменную окружения Expo:

```bash
EXPO_PUBLIC_API_URL=https://yourdomain.com
```

### 4. Сборка приложения

```bash
# Для Android
npx expo build:android

# Для iOS
npx expo build:ios
```

### 5. Запуск сервера

```bash
# С uvicorn
uvicorn main:app --host 0.0.0.0 --port 8001

# Или с gunicorn (рекомендуется для продакшена)
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8001
```

### 6. Настройка Nginx (рекомендуется)

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 7. SSL сертификат (обязательно для продакшена)

Используйте Let's Encrypt:

```bash
sudo certbot --nginx -d yourdomain.com
```

---

## ⚠️ Важные замечания

### Безопасность

1. **Никогда не коммитьте `.env` файл в Git!**
   - Убедитесь, что `.env` в `.gitignore`

2. **CORS должен быть ограничен:**
   - В продакшене укажите конкретные домены в `ALLOWED_ORIGINS`
   - Не используйте `["*"]` в продакшене!

3. **API ключи:**
   - Все ключи должны быть в `.env`
   - Не храните ключи в коде

### Производительность

1. **База данных:**
   - SQLite подходит для небольших проектов
   - Для больших нагрузок рассмотрите PostgreSQL

2. **Rate Limiting:**
   - Настроен на 10 загрузок/мин и 30 запросов/мин для чата
   - Можно настроить под ваши нужды

3. **Изображения:**
   - Автоматическая оптимизация работает
   - Максимальный размер файла: 10 MB

---

## 🔍 Проверка перед деплоем

- [ ] Все переменные окружения настроены
- [ ] API_URL указан правильно
- [ ] CORS ограничен конкретными доменами
- [ ] `.env` файл не в Git
- [ ] SSL сертификат установлен
- [ ] Rate limiting работает
- [ ] Логирование настроено
- [ ] База данных создана и мигрирована

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи сервера (`logger.info`, `logger.error`)
2. Переменные окружения
3. CORS настройки
4. Firewall правила

---

**Готово к продакшену!** 🎉


# ⚡ Швидкий деплой DikorosUA в Google Play

## 🚀 Команди для швидкого старту

### 1️⃣ Перший деплой (Internal Testing)

```bash
# 1. Встановіть EAS CLI (якщо ще не встановлено)
npm install -g eas-cli

# 2. Увійдіть в Expo аккаунт
eas login

# 3. Налаштуйте credentials (тільки перший раз)
eas credentials

# 4. Створіть production build
eas build --platform android --profile production

# 5. Дочекайтеся завершення (10-20 хвилин)
# Скачайте .aab файл з посилання в терміналі
```

### 2️⃣ Оновлення версії

**Перед кожним новим build:**

1. Відкрийте `app.json`
2. Змініть:
```json
"version": "1.0.1",  // Збільште версію
"android": {
  "versionCode": 5   // Збільште на 1
}
```

### 3️⃣ Створення нового релізу

```bash
# Після оновлення версії в app.json:
eas build --platform android --profile production
```

### 4️⃣ Тестовий build (APK для швидкого тестування)

```bash
# Створення APK для локального тестування
eas build --platform android --profile preview
```

---

## 📋 Чеклист перед build

- [ ] Версія оновлена в `app.json`
- [ ] `versionCode` збільшено
- [ ] Всі зміни закомічені в git
- [ ] Локальне тестування пройдено
- [ ] `npm install` виконано

---

## 🔗 Корисні посилання

- **EAS Builds**: https://expo.dev/accounts/[your-account]/projects/dikorosua/builds
- **Google Play Console**: https://play.google.com/console
- **EAS Documentation**: https://docs.expo.dev/build/introduction/

---

## ⚠️ Типові помилки

### "Build failed: Gradle error"
```bash
# Очистіть кеш та спробуйте знову
eas build --platform android --profile production --clear-cache
```

### "Version code must be greater than previous"
- Збільште `versionCode` в `app.json`

### "Invalid keystore"
```bash
# Перевірте credentials
eas credentials
```

---

## 📊 Моніторинг build

```bash
# Перегляд статусу поточного build
eas build:list

# Перегляд логів конкретного build
eas build:view [BUILD_ID]
```

---

## 🎯 Автоматизація (майбутнє)

Після налаштування Service Account можна використовувати:

```bash
# Build + автоматична публікація в Internal Testing
eas build --platform android --profile production --auto-submit
```

**Для цього потрібно:**
1. Створити Service Account в Google Cloud Console
2. Додати `google-play-service-account.json` в корінь проекту
3. Налаштування вже є в `eas.json`

---

## 💡 Поради

1. **Завжди тестуйте локально** перед створенням production build
2. **Використовуйте preview profile** для швидкого тестування APK
3. **Зберігайте release notes** для кожної версії
4. **Не забувайте про versionCode** - він повинен завжди зростати

---

## 📞 Підтримка

Якщо щось не працює:
1. Перевірте логи build: `eas build:view [BUILD_ID]`
2. Перегляньте документацію: https://docs.expo.dev/eas/
3. Перевірте форуми: https://forums.expo.dev/

**Успіхів! 🚀**
